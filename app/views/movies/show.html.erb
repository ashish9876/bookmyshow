Name = <%= @movie.name %>

<%= link_to "Delete ", movie_path , method: :delete, data: { confirm: 'Are you sure you want to delete? This is permanent.' } if current_user.has_role? :admin %>
<br>
<%= link_to "Book Tickets", theatres_path %>
</br>
Category = <%= @movie.category %>
<%= image_tag @movie.avatar %>
